package migration

import (
	"time"

	"github.com/jmoiron/sqlx"
)

func init() {
	migrator.AddMigration(&Migration{
		ID:        0,
		Name:      "{{.Version}}_{{.Name}}",
		Batch:     0,
		CreatedAt: time.Time{},
		Up:        mig_{{.Version}}_{{.Name}}_up,
		Down:      mig_{{.Version}}_{{.Name}}_down,
	})
}

func mig_{{.Version}}_{{.Name}}_up(tx *sqlx.Tx) error {
	return nil
}

func mig_{{.Version}}_{{.Name}}_down(tx *sqlx.Tx) error {
	return nil
}